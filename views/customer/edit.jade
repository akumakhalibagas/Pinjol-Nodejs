extends ../layout/base 
block content 
	section.content-header 

	section.content
		.box
			.box-header.with-border
				h3.box-title
					| Zallibo Pinjaman Online
				.box-tools.pull-right
					button(type='button', data-widget='collapse', data-toggle='tooltip',title='collapse').btn.btn-box-tool
						i.fa.fa-minus
					
			.box-body
				form(role='form',method='post' action='/customers/edit/#{(id==undefined) ? data.id : id}')
					.row
						
						.col-xs-6
							.box-body
								- if (messages.msg_error) 
									.alert.alert-danger.alert-dismissable 
										button.close(type='button', data-dismiss='alert', aria-hidden='true') 
											| ×
										h4
											i.icon.fa.fa-ban
											| Alert! 
										| !{messages.msg_error}
								- if (messages.msg_info) 
									.alert.alert-success.alert-dismissable 
										button.close(type='button', data-dismiss='alert', aria-hidden='true') 
											| ×
										h4
											i.icon.fa.fa-ban
											| Success! 
										| !{messages.msg_info}
								.form-group
									label
										| Nama
									input(type='text',value="#{data.name}",name='name',placeholder='Nama', autofocus).form-control
								.form-group
									label
										| alamat
									input(type='text',value="#{ (address == undefined) ? data.address : address }",name='address',placeholder='alamat').form-control
								.form-group
									label
										| jumlah pinjaman
									input(type='text',value="#{ (email == undefined) ? data.email : email }",name='email',placeholder='jumlah pinjaman').form-control
								.form-group
									label
										| jangka waktu
									input(type='text',value="#{ (phone == undefined) ? data.phone : phone }",name='phone',placeholder='jangka waktu').form-control
							.box-footer
								input(type="hidden",name="_method", value="PUT")
								button(type='submit').btn.btn-primary
									i.fa.fa-pencil 
									|  Edit
								a(href='/customers').btn.btn-primary.pull-right
									| Back